# Pinned upstream Restful Booker Platform source (https://github.com/mwinteringham/restful-booker-platform).
# Every service in its docker-compose.yml is build-based (no published image set is guaranteed
# mutually compatible), so we pin an exact commit and build it ourselves. See localSutPrepare
# in build.gradle: it fetches this exact commit into build/rbp-sut/<commit> on demand.
repository=https://github.com/mwinteringham/restful-booker-platform.git
commit=d36bd3f8647a091d406e53bad463c5e3e5d2ece1
